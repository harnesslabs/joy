# Manifest Schema

This chapter documents the public `joy.toml` schema used by project, workspace, and package manifests.

Source of truth is `src/manifest.rs`. Keep this chapter synchronized with shipped parser/validator behavior.
