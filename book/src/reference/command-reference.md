# Command Reference

This chapter is the command-level map for shipped `joy` CLI behavior.

Use workflow chapters for task-first guidance, and this chapter when you need fast command/flag lookup across command families.
