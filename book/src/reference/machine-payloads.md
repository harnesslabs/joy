# Machine Payload Matrix

This chapter catalogs command payload shapes emitted by `--json` / `--machine` mode.

Machine interface envelope policy is documented in [Machine Interface](machine-interface.md). This chapter focuses on command-specific payload keys.
