# Registry Discovery and Configuration

`joy` supports registry configuration and discovery commands:

- `joy registry list|add|remove|set-default`
- `joy search <query>`
- `joy info <package>`

This chapter documents practical setup and troubleshooting flow, while [Registry Configuration](../reference/registry-config.md) captures the precise config contract.
