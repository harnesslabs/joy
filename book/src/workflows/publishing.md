# Package Publishing and Ownership

`joy` supports self-hosted package publishing workflows:

- `joy package init`
- `joy publish`
- `joy owner list|add|remove`
- `joy yank [--undo]`

This chapter covers end-to-end usage and operational guardrails. Schema details for registry metadata and machine output live in the reference section.
