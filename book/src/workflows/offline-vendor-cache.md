# Offline Fetch, Vendor, and Cache

`joy` supports explicit offline-first preparation workflows:

- `joy fetch`
- `joy vendor`
- `joy cache gc`

Use this chapter for task-driven flows and CI patterns. Reference payload keys are documented in [Machine Payload Matrix](../reference/machine-payloads.md).
