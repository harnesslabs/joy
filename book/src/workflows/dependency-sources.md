# Dependency Source Backends

`joy` supports multiple dependency source backends, each with explicit manifest semantics.

This chapter describes the user workflow for:

- `github` dependencies
- `registry` dependencies
- `git` dependencies
- `path` dependencies
- `archive` dependencies

See [Manifest Schema](../reference/manifest-schema.md) for exact field-level contract details.
